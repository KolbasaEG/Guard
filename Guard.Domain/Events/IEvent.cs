namespace Guard.Domain.Events
{
  /// <summary>
  /// Базовый интерфейс для всех событий в доменной модели.
  /// </summary>
  public interface IEvent
  {
    // Пустой интерфейс, используется только для маркировки событий.
  }
}